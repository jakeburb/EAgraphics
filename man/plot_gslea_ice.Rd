% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FunctionsN.R
\name{plot_gslea_ice}
\alias{plot_gslea_ice}
\title{Plot GSLEA Ice and Seasonal Phenology by EAR}
\usage{
plot_gslea_ice(
  data,
  var,
  EARs = 0,
  groups = NULL,
  year_range = c(1990, 2023),
  lang = "en",
  fit_smooth = FALSE,
  show_line = TRUE,
  method = "gam",
  formula = y ~ s(x, bs = "cs", k = 15),
  col_palette = NULL,
  ear_names = NULL,
  xlab = NULL,
  ylab = NULL,
  base_size = 14,
  facet_scales = "free_y",
  custom_theme = ggplot2::theme_bw()
)
}
\arguments{
\item{data}{Long-format data frame containing \code{year}, \code{EAR}, \code{variable}, and \code{value}.}

\item{var}{Ice/seasonal variable to plot (unquoted, e.g., \code{ice.duration}).}

\item{EARs}{Vector of EAR identifiers. Defaults to \code{0}.}

\item{groups}{Optional named list to aggregate EARs.}

\item{year_range}{Numeric vector \code{c(start, end)}. Defaults to \code{c(1990, 2023)}.}

\item{lang}{Language for labels: \code{"en"} or \code{"fr"}.}

\item{fit_smooth}{Logical. If \code{TRUE}, adds a GAM smoother.}

\item{show_line}{Logical. If \code{TRUE}, connects annual points with a line. Defaults to \code{TRUE}.}

\item{method}{Smoothing method. Defaults to \code{"gam"}.}

\item{formula}{Smoothing formula. Defaults to \code{y ~ s(x, bs = "cs", k = 15)}.}

\item{col_palette}{Optional character vector of colors for the regions.}

\item{ear_names}{Optional named vector to override EAR numbers with names.}

\item{xlab, ylab}{Optional custom axis labels.}

\item{base_size}{Base font size for the plot. Defaults to \code{14}.}

\item{facet_scales}{Character. Control facet scales: \code{"free_y"} (default) or \code{"fixed"}.}

\item{custom_theme}{A ggplot2 theme. Defaults to \code{theme_bw()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes ice and seasonal timing metrics from the gslea package.
Supports individual EARs or custom aggregated groups with controllable scales.
Can visualize with points connected by line or with a smoothed GAM.
}
\examples{
\dontrun{
# Compare ice duration between Northern and Southern groups with lines and points
plot_gslea_ice(EA.data, ice.duration,
               year_range = c(2000, 2020),
               groups = list("Northern" = 1:4, "Southern" = 5:6),
               lang = "fr",
               show_line = TRUE)
}
}
