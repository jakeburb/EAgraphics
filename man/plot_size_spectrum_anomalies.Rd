% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FunctionsN.R
\name{plot_size_spectrum_anomalies}
\alias{plot_size_spectrum_anomalies}
\title{Plot Size Spectrum Anomalies}
\usage{
plot_size_spectrum_anomalies(
  data,
  lang = "en",
  year_range = NULL,
  x_breaks = NULL,
  base_size = 14,
  color_limits = c(-3, 3)
)
}
\arguments{
\item{data}{A data frame containing 'year', 'EAR', 'variable', and 'value'.}

\item{lang}{Language for labels: \code{"en"} (default) or \code{"fr"}.}

\item{year_range}{Optional numeric vector \code{c(start, end)} to set a fixed timeline.
Defaults to the full range of the data.}

\item{x_breaks}{Optional numeric vector for x-axis tick marks.}

\item{base_size}{Numeric. Base font size. Defaults to 14.}

\item{color_limits}{Numeric vector of length 2 for the anomaly scale. Defaults to \code{c(-3, 3)}.}
}
\value{
A \code{patchwork} object with two stacked panels (A and B).
}
\description{
Creates a two-panel stacked heatmap of size spectrum anomalies for EAR 100 and 200.
Automatically extracts size classes from variable names (e.g., 'from15to25' to '[15-25[')
and ensures perfectly aligned x-axes even with different start dates.
}
\examples{
\dontrun{
# 1. Basic plot with automatic year range
plot_size_spectrum_anomalies(size.spectrum.data.gsl)

# 2. French version with a fixed timeline and custom breaks
plot_size_spectrum_anomalies(size.spectrum.data.gsl,
                             lang = "fr",
                             year_range = c(1985, 2025),
                             x_breaks = seq(1985, 2025, 5))
}
}
