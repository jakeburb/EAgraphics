% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FunctionsN.R
\name{plot_stock_status}
\alias{plot_stock_status}
\title{Plot Stock Status Relative to Limit Reference Point (LRP)}
\usage{
plot_stock_status(
  data = NULL,
  year_col = Year,
  stock_col = Stock,
  status_col = status.LRP,
  lang = "en",
  year_range = NULL,
  show_title = TRUE,
  base_size = 16
)
}
\arguments{
\item{data}{A data frame containing stock status data. Defaults to
\code{gslea::EA.extra.data$stock.status}.}

\item{year_col}{Unquoted name of the column containing the year.
Defaults to \code{Year}.}

\item{stock_col}{Unquoted name of the column containing stock names.
Defaults to \code{Stock}.}

\item{status_col}{Unquoted name of the column containing status values
relative to LRP. Defaults to \code{status.LRP}.}

\item{lang}{Character string for language: \code{"en"} (English, default)
or \code{"fr"} (French).}

\item{year_range}{Optional numeric vector \code{c(start, end)} to filter
the years shown.}

\item{show_title}{Logical. Should the main plot title be displayed? Defaults to \code{TRUE}.}

\item{base_size}{Numeric. Base font size for the theme. Defaults to 14.}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates a standardized heatmap visualizing stock status over time.
Features colour-coded status zones. Includes bilingual support (English/French) with an internal
dictionary to ensure consistent Y-axis ordering regardless of the language selected.
}
\examples{
\dontrun{
library(EAgraphics)

# Example 1: Standard plot with title in English
plot_stock_status(lang = "en")

# Example 2: Plot without title for use in multi-panel figures or reports
plot_stock_status(lang = "en", show_title = FALSE)

# Example 3: Plot without title and in french with year 1990-2025
plot_stock_status(lang = "fr", year_range = c(1990, 2025), show_title = FALSE, base_size=20)
}
}
