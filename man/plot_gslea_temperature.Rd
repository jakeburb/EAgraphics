% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FunctionsN.R
\name{plot_gslea_temperature}
\alias{plot_gslea_temperature}
\title{Plot Temperature data from gslea by EAR}
\usage{
plot_gslea_temperature(
  data,
  var,
  EARs = 0,
  groups = NULL,
  years = c(1990, 2023),
  lang = "en",
  fit_smooth = TRUE,
  method = "gam",
  formula = y ~ s(x, bs = "cs", k = 15),
  col_palette = NULL,
  ear_names = NULL,
  xlab = NULL,
  ylab = NULL,
  base_size = 14,
  facet_scales = "free_y",
  custom_theme = ggplot2::theme_bw()
)
}
\arguments{
\item{data}{Long-format data frame containing \code{year}, \code{EAR}, \code{variable}, and \code{value}.}

\item{var}{Temperature variable to plot (unquoted, e.g., \code{sst.month10}).}

\item{EARs}{Vector of EAR identifiers. Defaults to \code{0} (Entire Gulf).}

\item{groups}{Optional named list to aggregate EARs (e.g., \code{list("Northern" = 1:4)}).}

\item{years}{Numeric vector \code{c(start, end)}. Defaults to \code{c(1990, 2023)}.}

\item{lang}{Language for labels: \code{"en"} or \code{"fr"}.}

\item{fit_smooth}{Logical. If \code{TRUE}, fits a GAM smoother.}

\item{method}{Smoothing method. Defaults to \code{"gam"}.}

\item{formula}{Smoothing formula. Defaults to \code{y ~ s(x, bs = "cs", k = 15)}.}

\item{col_palette}{Optional character vector of colors for the regions.}

\item{ear_names}{Optional named vector to override EAR numbers with names.}

\item{xlab, ylab}{Optional custom axis labels.}

\item{base_size}{Base font size for the plot. Defaults to \code{14}.}

\item{facet_scales}{Character. Control facet scales: \code{"free_y"} (default) or \code{"fixed"}.}

\item{custom_theme}{A ggplot2 theme. Defaults to \code{theme_bw()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes temperature data from the gslea package using long-format data.
Supports individual EARs or custom aggregated groups with automatic
translation and unit detection.
}
\details{
This function streamlines GSL temperature visualizations. It detects
temperature metrics to append "(°C)" and uses an internal bilingual
dictionary. Custom groups provided via \code{groups} are aggregated by annual
mean and translated.
}
\examples{
\dontrun{
# Compare regions with a fixed Y-axis scale and custom colors
my_regions <- list("Northern" = 1:4, "Southern" = c(5, 6, 50))
plot_gslea_temperature(EA.data, sst.month10,
                       groups = my_regions,
                       facet_scales = "fixed",
                       col_palette = c("Northern" = "royalblue", "Southern" = "firebrick"))
}
}
